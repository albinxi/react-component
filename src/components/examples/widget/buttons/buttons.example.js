import React from 'react';
import * as _ from 'lodash';

import {
  ButtonComponent,
} from '../../../button/button';

export class ButtonExample extends React.Component {
  constructor(props) {
    super(props);
  }

  render() {
    return (
      <div>Button component examples is in progress ...</div>
    );
  }
}
