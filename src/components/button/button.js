import React from 'react';
import * as _ from 'lodash';

import './button.scss';

export class ButtonComponent extends React.Component {
  constructor(props) {
    super(props);
  }

  render() {
    return (
      <button
        className={`button`}
      >

      </button>
    );
  }
}
